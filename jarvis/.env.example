# Copy to .env and fill in the keys you have. Only one LLM key is required.
# .env is gitignored — never commit real keys.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
PICOVOICE_ACCESS_KEY=
